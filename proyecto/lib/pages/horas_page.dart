import 'package:flutter/material.dart';

class HorasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('⏰ Horas Page', style: TextStyle(fontSize: 24)));
  }
}
